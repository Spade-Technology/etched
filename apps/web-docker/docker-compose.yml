services:
  postgres:
    image: postgres:17-alpine
    restart: always
    env_file:
      - .env.docker
    ports:
      - "7772:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
# volumes:
#   postgres_data:
